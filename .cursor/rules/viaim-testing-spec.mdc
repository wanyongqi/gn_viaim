---
alwaysApply: false
description: viaim蓝牙耳机App功能测试用例设计规范（Android/iOS）
---
# viaim功能测试用例设计规范
本规则用于在为viaim这款可通过蓝牙协议连接蓝牙耳机的App（支持 Android 与 iOS）设计功能测试用例时启用。

详细输出规范见 skill：[test-case-output-requirements/reference.md](mdc:.cursor/skills/test-case-output-requirements/reference.md)，以下为核心摘要与项目补充说明。

## 输出格式与结构
- 使用 Markdown 语法输出用例。
- 用例字段包含：测试项、用例名称、优先级、前置条件、操作步骤、预期结果。
- **单个需求文件默认使用列表树结构输出，不使用 `#`、`##`、`###` 标题符号。**
- 默认层级：`- 功能点名称` → `- 【一级分组】` → `- 【二级分组】` → `- tc-Px：用例标题` → `- pc:` → `- 步骤` → `- 预期结果`
- 分级分组名称须用 `【】` 括号标注，不允许写成普通说明文本。

## 标题与文案
- 用例名称前缀必须为 `tc-优先级`，例如：`tc-P0：通话录音暂停后继续恢复录制`
- 标题使用主谓宾句式，直接描述"什么场景下什么行为结果正确/异常"。
- 前置条件使用 `pc:` 前缀，浓缩为一句状态句，优先用"已进入 / 位于 / 已选中 / 已打开 / 已触发"等状态描述，不重复写无意义动作过程。
- 单条用例最多不超过 3 个步骤，优先控制在 2 个步骤。
- 一条用例只承载 1 个核心断言，不把两个独立验证目标塞进同一条；如同时覆盖"交互是否正常"和"修改后是否生效"等独立目标，优先拆开写。
- `pc:` 写状态，步骤写动作，预期写可观察结果。
- **文案风格统一**：统一使用 `toast`、`弹窗`、`横幅`、`kill APP`、`切后台`、`最小化后切回前台` 等固定术语，避免同义词混用。

## 文件命名与输出位置
- 单个需求输出为独立 `md` 文件，文件名格式：`需求名称_测试用例.md`
- 若需求属于某一版本，默认输出到对应版本目录，例如：`630版本/test case/xxx_测试用例.md`
- 如需汇总多个需求，再额外生成汇总文件；汇总文件中，每个来源需求的首行功能点名称可转为 Markdown 一级标题 `# 功能点名称`；单个需求文件仍保持列表树格式。

## 拆分与合并规则
- 录音类型（通话录音 / 现场录音 / 音视频录音）默认分开写，除非用户明确允许合并。
- Android / iOS 默认视为功能一致，除非系统限制、交互差异或需求明确指出，否则不区分用例。
- 若需求点要求覆盖多维度，优先使用"分组下沉"而不是在单条标题中混杂多个维度。
- 用户若明确要求"3 大录音用例分开写""不要强行合并用例"等，需优先遵守，不得为压缩数量而牺牲清晰度。

## 优先级定义
- `P0`：核心功能的正向测试用例。
- `P1`：基本功能的常用场景，包含重要的错误与边界值。
- `P2`：基本功能的不常用场景。
- `P3`：基本功能的反向测试（异常、边界）。
- 优先级目标比率：`P0` 约 15%，`P1` 约 30%，`P2` 约 35%，`P3` 约 20%。
- 比率按单个需求文件控制，允许小幅浮动；文件体量较小时，不为凑比例而强行加删用例。

## 覆盖范围
- 覆盖所有符合真实用户场景的情况与全部功能点。
- 尽可能详尽覆盖异常场景，确保功能稳定。
- 关注交互操作准确性及 UI 展示与设计稿一致性。
- 除主流程外，主动考虑：断网、失败、切后台、最小化、kill APP、重进页面、重试、状态恢复、锁屏息屏等场景；确实不适用时跳过。
- 涉及语种能力时，默认参考 `docs/source-data/语种优先级_可读版.md` 选择覆盖样本；除正向语种外，还需关注混合语种、识别失败、无内容、断网等场景。

## 设计方法（按需适用）
- 等价类划分、边界值分析、场景法、错误推测法、决策表法、状态转换法、流程图法。
- 还需考虑：性能测试、安全测试以及系统平台特性（Android / iOS）。
- 每个需求点均考虑上述方法；不适用则跳过，保持简洁。

## 需求分析流程
- 等待用户给出需求后开始分析，不明确处必须先提问澄清。
- 禁止新增、修改或曲解需求意思。
- 提问后等待用户回复，再次完善需求分析，并等待用户最终"开始生成"的指令后再产出用例。
- 若用户后续补充或修改需求，以最新口径为准；若已有用例与最新逻辑冲突，应调整或删除旧用例，不保留互相矛盾的两套表达。

## 产出注意事项
- 面向viaim真实蓝牙连接与耳机相关功能场景（配对、连接、断连、重连、编解码、音视频、通话、固件升级、权限、异常恢复等）。
- 分别考虑Android与iOS在权限、系统交互上的差异与兼容性；功能一致时不区分用例，有系统差异时须拆开。
- 如用户需要，可额外输出用例总数、优先级分布、补漏建议，或"需求点 → 用例标题 / 数量"对照清单。
