---
alwaysApply: false
description: viaim迭代版本测试回归重点建议规范
---
# 测试回归重点建议规范

本规则用于在新版本发布前，根据本次迭代的需求变更点，自动判断哪些已有功能需要纳入回归，并给出回归范围和优先级建议。

详细操作流程见 skill：[regression-focus/SKILL.md](mdc:.cursor/skills/regression-focus/SKILL.md)

## 触发时机
- 用户提供本次迭代的需求点或测试用例后，主动识别对存量功能的影响范围。
- 用户明确要求"给出回归建议""哪些需要回归""回归重点"时触发。

## 判断原则
- **直接影响**：本次改动直接涉及的功能模块，必须纳入回归。
- **间接影响**：改动可能波及的上下游功能（如公共组件、录音状态机、语言选择逻辑），需判断是否纳入。
- **历史高风险**：过往版本曾出现过问题的功能点，若本次改动触及相关代码路径，优先级上调。
- **用户强感知**：直接面向用户的核心主流程（录音启停、转写上屏、导出等），无论是否有直接改动，每版必回归。

## 回归优先级定义
- **必回归（P0）**：改动直接影响，或核心主流程强感知功能。
- **建议回归（P1）**：改动间接影响，或历史高风险功能。
- **可选回归（P2）**：低频边缘场景，改动关联度弱，按资源决定是否覆盖。

## 输出格式
- 按功能模块分组，标注回归优先级（必回归 / 建议回归 / 可选回归）。
- 每条说明影响原因，不只列结论。
- 格式参考：`- **功能模块**（必回归）：原因说明`
- 可选附上"本版不影响、无需回归"的功能清单，减少测试遗漏和冗余。

## 禁止行为
- 不得无依据地将所有功能列为"必回归"。
- 不得忽略间接影响的功能模块。
- 不得自行扩大或缩小需求范围。
